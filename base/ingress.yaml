apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ticket-sale
  namespace: app
spec:
  ingressClassName: nginx
  rules:
    - host: ticket-sale.staging.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: api.ticket-sale.staging.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-service
                port:
                  number: 3030