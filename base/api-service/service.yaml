apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: app
spec:
  selector:
    app: api-service
  ports:
    - port: 3030
      name: http
      targetPort: http
    - port: 9464
      name: metrics
      targetPort: metrics