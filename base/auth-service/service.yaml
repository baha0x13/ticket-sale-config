apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: app
spec:
  selector:
    app: auth-service
  ports:
    - port: 4000
      name: http
      targetPort: http
    - port: 9464
      name: metrics
      targetPort: metrics